package com.soteria.assurance

import android.app.Application

/**
 * Firebase is auto-initialized from `google-services.json` via the
 * google-services Gradle plugin, so no manual init is required here.
 */
class SoteriaApp : Application()
