{
  "name": "learner",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/learner/src",
  "tags": ["scope:learner", "type:app"],
  "targets": {
    "build": {
      "executor": "@angular-devkit/build-angular:application",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/learner",
        "index": "apps/learner/src/index.html",
        "browser": "apps/learner/src/main.ts",
        "polyfills": ["zone.js"],
        "tsConfig": "apps/learner/tsconfig.app.json",
        "assets": [{ "glob": "**/*", "input": "apps/learner/public" }],
        "styles": ["apps/learner/src/styles.scss"],
        "scripts": []
      },
      "configurations": {
        "production": {
          "budgets": [
            { "type": "initial", "maximumWarning": "1mb", "maximumError": "3mb" },
            { "type": "anyComponentStyle", "maximumWarning": "4kb", "maximumError": "10kb" }
          ],
          "outputHashing": "all",
          "serviceWorker": "apps/learner/ngsw-config.json"
        },
        "development": {
          "optimization": false,
          "extractLicenses": false,
          "sourceMap": true,
          "fileReplacements": [
            {
              "replace": "apps/learner/src/environments/environment.ts",
              "with": "apps/learner/src/environments/environment.development.ts"
            }
          ]
        }
      },
      "defaultConfiguration": "production"
    },
    "serve": {
      "executor": "@angular-devkit/build-angular:dev-server",
      "configurations": {
        "production": { "buildTarget": "learner:build:production" },
        "development": { "buildTarget": "learner:build:development" }
      },
      "defaultConfiguration": "development"
    },
    "lint": { "executor": "@nx/eslint:lint" },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": { "jestConfig": "apps/learner/jest.config.ts" }
    }
  }
}
